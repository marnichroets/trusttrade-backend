# Production backend (Railway). Vercel also sets REACT_APP_API_URL to this same host,
# which takes precedence in src/utils/api.js — this keeps the local fallback consistent
# so the app can never silently fall back to a dead host.
REACT_APP_BACKEND_URL=https://trusttrade-backend-production-3efa.up.railway.app
